# 毕设选题思路：面向成本优化的多智能体编程自动编排平台

> 版本 v2.3 · 2026-09-24  
> 用途：开题报告“选题依据、研究问题与创新点”的素材  
> 配套文档：《毕设设计方案-多智能体编程系统.md》

---

## 一、一句话定位

设计并实现一个带有**自适应多智能体编排能力**的桌面 Coding Agent：普通问答和简单开发保持单 Agent；用户可以像 DSH 的 `agent-teams` 一样显式选择多智能体 Skill，也可以由入口路由器在任务复杂、可拆成多个独立工作流且预期收益高于协调开销时自动开启多智能体。进入该模式后，平台生成任务 DAG，并根据子任务的类型、复杂度、风险、模型能力、价格和历史表现，动态选择合适的 Agent 角色、模型、工具与执行策略。

系统追求的不是“永远调用最便宜的模型”，而是：

> 在满足任务质量要求的前提下，尽可能降低完成一个成功任务所需的总成本。

### 多智能体入口门控（强制规则）

多智能体不是所有任务的默认执行方式。平台同时支持“显式强制”和“复杂度自动判断”，但自动判断不能只依赖一个模糊的“任务很难”标签，而要评估任务是否真正适合拆分。

```text
用户选择 MultiAgentSkill / 明确要求多个 Agent
   └──────────────────────────────────────────────→ FORCE_MULTI

普通请求 → ComplexityGate
              ├─ 高收益、高置信度、至少 2 个独立工作流 → AUTO_MULTI
              ├─ 收益或置信度处于灰区                 → ASK_USER
              └─ 简单、串行、同文件冲突高             → SINGLE_AGENT
```

- `SINGLE_AGENT`：由主对话 Agent 直接理解需求、使用工具和编写代码；不创建子 Agent，不生成任务 DAG，也不调用多智能体路由器。
- `FORCE_MULTI`：用户选择多智能体 Skill 或明确要求多 Agent，直接进入多智能体规划。
- `AUTO_MULTI`：只有在复杂度、可并行度、上下文隔离收益、文件冲突风险和预期成本收益均通过阈值后才自动进入；界面显示“已自动启用”及原因，并允许立即撤销。
- `ASK_USER`：系统认为可能受益但证据不足时，只提出一次轻量确认，不预先创建子 Agent。
- “文件多”或“预计耗时长”只能作为特征，不能单独触发。复杂但高度串行、多个步骤修改同一文件、协调成本过高的任务仍由单 Agent 或单个强模型完成。
- 自动或显式多智能体都只对当前任务有效；新任务重新经过入口门控。用户可随时停止多智能体并回到主对话。

自动开启多智能体**不**调用主模型或分配模型做预判，只用发送前可得的规则特征（独立工作流数、路径簇是否重叠、是否强串行等）。推荐判据：

```text
AutoMulti = adaptive_mode_enabled
            AND independent_workstreams >= 2
            AND file_overlap_low
            AND NOT strong_serial_signal
            AND gate_confidence >= τ_conf
```

灰区用一次 UI 确认（`ASK_USER`），不预创建 DAG。进入多智能体之后，才由**分配器**读取用户配置的模型价签、公开能力与可选历史记录，为各子任务选模型（详见设计方案 §7.2）。

---

## 二、问题是如何发现的

现有 Coding Agent 已经能够使用工具、修改代码、运行测试，并在一定程度上调用子 Agent。问题在于，多 Agent 能力并不天然等于高效编排。

对于普通问答、小范围代码修改和日常开发，单 Agent 通常已经足够；若平台不区分执行模式，反而会为所有请求增加拆解、路由、上下文传递和结果汇总的额外成本。因此本项目先解决“这个任务是否值得进入多智能体模式”，再研究进入该模式后“每个子任务应交给谁”。

实际使用中经常出现两种模式：

1. 根 Agent 与子 Agent 共用同一个模型，简单的搜索、整理和样板代码也消耗高能力模型；
2. 开发者提前定义多个 Agent，并为每个 Agent 静态指定模型，随后由用户或主 Agent选择调用哪个角色。

第二种方式虽然允许混合模型，但“角色—模型—任务”的映射通常在运行前已经写死。例如：

```text
小王 = 调研 Agent = 低成本模型
小张 = 编程 Agent = 中等模型
小李 = 审查 Agent = 高能力模型
```

这种绑定仍然不够合理，因为同一角色面对的任务难度可能完全不同：查找一个函数很简单，调研陌生框架的并发机制却很复杂；修改变量名很简单，修改权限或事务逻辑则风险很高。

因此，本项目不把“Agent 名称”作为固定的执行资源，而是将三个概念解耦：

```text
角色：以什么职责完成任务
模型：使用什么能力、成本和上下文规格
工具：允许执行哪些读写、测试和外部操作
```

平台在每个子任务开始前动态组合三者，形成一次具体的执行配置。

---

## 三、对现有能力的准确判断

本项目不能以“所有现有产品都无法给子 Agent 指定模型”为立论，因为这一说法并不准确。

- OpenAI Responses API 的托管多智能体模式中，同一 Agent 树共享请求模型；
- OpenAI Agents SDK 支持给不同 Agent 显式配置不同模型；
- Claude Code 支持在自定义子 Agent 中设置模型，也能根据 Agent 描述进行一定程度的自动委派。

主流 Coding Agent 对“何时启用子 Agent”的处理可以归纳为两层：

| 产品 | 自动委派方式 | 控制与边界 | 对本项目的启发 |
|---|---|---|---|
| Cursor | Agent 可根据任务复杂度、范围、子 Agent 描述和当前上下文自动委派 | 官方明确提醒简单任务会产生启动、Token 和延迟开销 | 复杂度可以触发，但必须计算收益并保留简单任务单 Agent 路径 |
| Claude Code | 根据请求描述、Agent 的 `description` 和上下文自动选择 subagent | 完整 agent teams 默认关闭；官方建议只用于可独立并行任务，并警告协调与 Token 开销 | 区分轻量委派与完整团队，完整 DAG 使用更高阈值 |
| Gemini CLI | 任务匹配专家描述时自动委派，也支持 `@agent` 强制调用 | 可全局关闭、按策略禁止某个 Agent，且禁止递归调用 | 自动路由与显式调用并存，策略层负责硬约束 |
| OpenAI Agents API | 会话先设置 `multi_agent.enabled=true`，启用后由协调 Agent 使用子 Agent 工具 | 官方建议短任务和强依赖步骤留在主 Agent | 将“能力是否可用”和“当前任务是否值得创建子 Agent”分开 |

因此，本项目采用混合方案：显式 Skill／用户指令可强制开启；普通请求先由低开销入口路由器评估复杂度、可并行度、冲突风险和预期净收益，再决定单 Agent、自动多 Agent或询问用户。

因此，真正值得研究的空白不是“能否配置多个模型”，而是：

> 能否根据每个编程子任务的实时特征，自动决定该调用哪个角色、哪个模型以及采用哪种执行策略，并利用测试、审查和历史运行结果持续修正调度决策？

也就是说，本项目研究的是**自动、动态、可反馈的任务路由**，而不是静态的多模型配置。

---

## 四、核心矛盾：便宜模型不一定真的省钱

最直观的成本优化方法是“先让便宜模型尝试，失败后再升级强模型”。但这一策略并不总是成立。

设低成本模型执行费用为 `C_l`，成功概率为 `p`；失败后调用强模型的费用为 `C_h`，整理并传递失败上下文的额外费用为 `C_t`。低成本优先策略的期望费用为：

```text
E = C_l + (1 - p) × (C_h + C_t)
```

只有当 `E < C_h` 时，低成本优先才比直接调用强模型划算。对于安全、并发、跨模块修改等高难任务，盲目让便宜模型先完整尝试，可能造成重复调用、错误修改和上下文污染。

因此平台必须在三类执行策略之间自动选择：

1. **低成本模型直接执行**：适用于代码搜索、资料整理、机械性修改等低风险任务；
2. **高能力模型直接执行**：适用于复杂调试、架构、安全和高影响范围任务；
3. **分阶段异构协作**：低成本模型完成代码定位、错误复现和事实提取，高能力模型负责关键分析与实现。

第三种策略中，前置 Agent 产生的是即使升级后仍然有价值的结构化证据，而不是一次可能被完全丢弃的廉价试错。

---

## 五、多执行会话、异构模型与外置项目记忆

子任务级动态分配必然带来两个工程事实，需要在设计中正面回答，而不是假设「子 Agent 自动继承父对话记忆」。

### 5.1 多执行会话（Multi-Session）指什么

在本项目中，**一次用户请求**背后可能存在多个彼此独立的 Agent 执行会话，例如：

- **主对话会话**：单 Agent 模式下的连续编程对话，或多 Agent 结束后的汇总；
- **Planner 会话**：只负责产出任务 DAG，通常关闭工具调用；
- **子任务会话**：DAG 中每个节点各自一次（或多次）执行，对应独立的会话持久化文件。

各会话由第三方执行骨架（如 pi 的 `SessionManager`）管理。**会话之间不会自动共享模型侧的对话状态**；若要让后续步骤「知道前面发生了什么」，必须由平台**显式组装**本次 prompt 中的上下文，而不是依赖 API 层的「记忆继承」。

### 5.2 换模型（异构路由）指什么

**换模型**指不同子任务（或同一会话内切换）使用不同的 `ModelProfile`（不同供应商、档位或端点）。不同模型的上下文窗口、工具协议与 **Prompt Cache 前缀**互不通用；因此平台不能把「父会话的内部状态」原样交给子模型，只能传递**可序列化的文本与结构化记录**。

这与本课题创新点一致：用角色—模型—工具解耦换取成本与能力匹配，代价是**不能假设**「整条链路始终同一模型、同一会话」。

### 5.3 为何不默认「继承父会话全文」

若每个子任务都附带完整父聊天记录：

- Token 与费用随子任务数量线性放大；
- 无关噪声增加，反而降低子 Agent 专注度；
- 各子任务 prompt 前缀差异大，不利于稳定前缀与缓存复用（若用户接入 Codex/OpenAI 兼容端点，仍依赖**稳定、可复用的固定块**）。

因此默认策略是：**传递「该子任务需要的最小充分上下文」**，而非全量对话克隆。

### 5.4 外置项目记忆（Project Memory）— 本项目的统一做法

平台在编排层维护**与 LLM 会话解耦**的 **外置项目记忆**，绑定到当前编排任务（或 `conversationId` / 工作区维度），由确定性代码读写；每次创建子任务执行实例前，从记忆中取出规定层级，注入 prompt。子任务结束后，由**归档步骤**写回记忆（规则摘要 + 可选轻量模型摘要）。

建议分层（由稳到变，便于论文描述与实现）：

| 层级 | 内容 | 更新时机 | 注入范围 |
|---|---|---|---|
| **L0 用户目标** | 用户原始需求的一句话锚点 | 编排开始 | 所有子任务 |
| **L1 滚动摘要** | 已完成工作、关键结论、未决问题 | 每个子任务结束 / 主对话里程碑 | 所有子任务（同一段文本利于稳定前缀） |
| **L2 依赖产出** | 直接前置子任务的结构化结果 | 依赖任务完成时 | 仅依赖它的子任务 |
| **L3 结构化事实** | 触及文件、命令、测试结论、风险标签 | 工具执行后提取 | 按任务类型选择性注入 |
| **L4 证据包** | 与设计方案中 `EvidenceBundle` 对齐，用于升级/重试 | 失败或升级时 | 仅升级路径 |

**不默认**将 L0～L3 替换为「父会话全文」；L4 与升级策略共用，避免失败噪声污染强模型。

```text
用户发送
  → 门控 / Planner（独立会话，可无工具）
  → 对每个子任务：
        read ProjectMemory(L0,L1,L2*,L3*)
        + TaskSpec + RouteDecision
        → 创建子会话 + 选定模型执行
        → writeback ProjectMemory / EvidenceBundle
  → 主会话汇总（可读 L1，不必读各子会话 jsonl）
```

### 5.5 与单 Agent 路径、第三方端点的关系

- **单 Agent**：仅一个执行会话，连贯性最好；外置记忆仍可用于跨天续作或压缩后恢复，但非必需。
- **多 Agent + 异构模型**：外置记忆是**连贯性的主通道**；子任务异构路由是创新点，**不**与「外置记忆」矛盾，而是配套设计。
- **用户自配的模型端点**（含官方 API、应用内 OAuth，或本机 OpenCodex 等 loopback 网关）：外置记忆不改变 API 路由；通过 **L1 等固定块 + 稳定 system/tools 前缀** 仍可在同模型、同会话形态下尽量利用服务商 Prompt Cache。多会话、多模型场景下缓存命中率通常低于「单端单会话长聊」，属于可接受的 **成本—能力权衡**，可在案例日志中用 `cacheRead` / `cacheWrite` 做说明。

### 5.6 对论文与实现的约束

- 记忆由**本项目编排层**实现与审计，不宣称对 pi 或模型供应商内部缓存机制的控制；
- 写回记忆时区分**已验证事实**与模型主观表述，与 `EvidenceBundle` 规则一致；
- 本科实现可先做 **L0 + L1 + L2**（L2 与现有「前置子任务结果」对齐），L3/L4 与升级闭环迭代。

---

## 六、为什么引入 Jev

如果使用普通生成式大模型来决定“下一步该调用哪个模型”，路由本身也会产生不可忽视的成本、延迟和不稳定的自由文本输出。

Jev 是 TypeSafe AI 提出的 System One 决策模型，其目标不是生成长文本，而是针对给定状态返回限定选项、评分、概率和置信度。它适合承担以下窄决策：

- 判断任务类型；
- 判断复杂度与风险；
- 在批准的候选模型中选择执行层级；
- 在继续、升级、拆分和人工介入之间选择；
- 当置信度不足时触发保守回退。

Jev 在系统中只是一个**可替换的路由引擎**，不负责写代码，也不直接拥有执行权限。最终决策还必须经过确定性策略检查，例如“安全任务不得使用最低等级模型”“低置信度必须走保守路线”。

考虑到 Jev 是新近发布的闭源托管服务，论文不能写成“接入 Jev API”。平台将定义统一的 `RoutingEngine` 接口，并至少实现：

- 规则路由器；
- Jev 路由器；
- 普通 LLM 路由器（对照）；
- 基于历史成功率的路由器（增强项）。

这样既降低供应商依赖，也能形成有意义的实验比较。

---

## 七、与 Multica、pi 的关系

### Multica：产品形态参照

本项目借鉴 Multica 的任务看板、Agent 状态、任务生命周期、成本统计和人工接管等平台思路，但不照搬其界面。

差异在于：Multica 更偏向管理已有 Coding Agent；本项目重点实现**任务感知的自动调度层**，自动决定任务应交给什么角色、模型与工具组合，并根据执行反馈重新路由。

### pi：单 Agent 执行骨架（架构参考 + npm 依赖，与 DSH/OpenClaw 同类）

执行层通过 npm 包 `@earendil-works/pi-coding-agent` 接入；**本仓库不包含 pi 源码**，仅在设计与实现上参考 pi 的分层（模型运行时、工具循环、extensions/skills）。TaskWeaver 通过 `electron/agent/agent-runtime.mjs` 统一适配，编排、门控、DAG、模型分配均为本项目代码。本项目自建：

- 需求拆解与任务 DAG；
- Agent／模型动态路由；
- **外置项目记忆（L0～L4）与多会话上下文装配**；
- 成本与质量策略；
- 状态机和任务生命周期；
- 验证、升级与重新调度；
- Git worktree 隔离；
- 审计、成本与实验统计。

这使第三方骨架和本人贡献之间保持清晰边界。

### 模型凭据与本机 OpenCodex 网关（可选功能板块）

为减少「在毕设里为 Cursor、Codex、Antigravity 各配一遍 Key」的负担，设计上支持用户本机已安装的 **OpenCodex**：由用户在 `ocx` 内完成各来源登录与 `sync`，TaskWeaver 通过 **loopback OpenAI 兼容端点** 接入 pi 自定义 provider（`ocx export --client pi` 合并 `models.json`），将 `cursor/…`、`google-antigravity/…`、Codex 侧模型等纳入**同一套 ModelProfile 与分配器**。

这与「直接读取 Cursor 登录态」不同：毕设**不**解析 Cursor 私有存储，只认用户显式启动的本机代理；与应用内 **Codex 订阅 OAuth** 并列，OAuth 为不依赖第三方代理的正式路径，OpenCodex 为可选增强。详细架构、阶段划分与风险见《毕设设计方案》**§4.2**。

---

## 八、系统如何自动分配任务

用户明确输入：

> 使用多智能体给现有项目增加登录失败次数限制并补充测试，优先控制成本。

Planner 首先生成任务 DAG：

```text
T1 查找认证相关代码
T2 分析现有登录与缓存流程
T3 设计失败次数限制方案
T4 修改登录逻辑
T5 编写测试
T6 安全审查与全量验证
```

路由器自动产生如下分配：

| 子任务 | 角色 | 模型层级 | 策略 |
|---|---|---|---|
| T1 代码定位 | Explorer | 低成本 | 直接执行 |
| T2 流程分析 | Explorer / Planner | 中等 | 直接执行 |
| T3 安全方案 | Planner | 高能力 | 直接执行 |
| T4 功能实现 | Executor | 中等 | 测试失败后可升级 |
| T5 测试编写 | Tester | 低或中等 | 根据接口复杂度选择 |
| T6 安全审查 | Reviewer | 高能力 | 强制高等级 |

用户不需要手动指定“小王做什么、小张做什么”。显式开启多智能体，或入口门控判断任务存在多个独立工作流且收益足够高后，系统自动完成任务拆解和资源选择；普通或高度串行的开发需求仍由主 Agent 直接完成。

---

## 九、论文要说明的问题（本科：设计 + 实现 + 测试，非大规模实验）

毕设不要求跑 SWE-bench 级对照或统计消融。论文重点说清楚**怎么做、为什么这样分层**，并用功能测试与 2～3 个固定演示案例证明能跑通；成本用审计日志中的 Token/估算费用做**案例分析**即可。

- **Q1**：门控（规则）与分配（读模型价签/能力卡）为何必须分开？单 Agent 路径为何零分配调用？
- **Q2**：子任务级动态分配相对「每角色静态绑一个模型」解决了什么问题？
- **Q3**：多执行会话与异构模型下，为何采用外置项目记忆（L0～L4）而非继承父会话全文？
- **Q4**：PolicyGate、测试与审查反馈如何约束分配并在失败时升级？
- **Q5**（可选）：演示案例中，动态分配是否比「全用强模型」表现出更低的估算总成本？

设计动机仍强调：优化的是**成功完成任务的总体成本**，而不是单次 API 单价；这一指标在毕设中通过案例日志说明，不作严格统计结论。

---

## 十、主要贡献点（开题陈述用，建议 3～4 条）

1. **角色、模型和工具解耦**：子任务执行前动态组合，而非静态「小王=便宜模型」；
2. **门控与分配分层**：规则门控决定是否多智能体；分配器（默认规则，可选 LLM）在用户维护的模型价签/能力卡上为子任务选模型；
3. **外置项目记忆**：在多执行会话与异构模型下，用 L0～L4 分层注入与写回保持任务连贯，并与 `EvidenceBundle` 升级路径统一；
4. **多策略执行与 PolicyGate**：cheap/balanced/strong 等策略 + 硬规则（如安全写入不得最低档）；
5. **验证驱动的重新路由**：测试/审查失败分类后的重试、升级与 EvidenceBundle；
6. （可选）**自适应入口**：显式 Skill + 规则 `AUTO_MULTI` / `ASK_USER`，不增加单 Agent 的模型开销。

不写「大规模对照实验」为贡献；测试与演示案例属于验收方式。

---

## 十一、明确不做什么

- 不训练新的代码大模型；
- 不把“接入 Jev”本身宣称为创新；
- 不复制完整的商业项目管理平台；
- 不追求支持所有开发任务；
- 不让低成本模型无条件先试；
- 不依据“文件多”“任务难”等单一信号无条件开启多智能体；
- 不把普通 Coding Agent 的每一次工具调用包装成子任务或子 Agent；
- 不把未经验证的推理链全部传给升级后的模型；
- 不自建大型任务数据集，用自备小仓库 + 2～3 个固定演示案例；
- 不做学术论文级大规模对照、消融、SWE-bench 排行榜复现；
- 不为入口门控调用执行级主模型做「要不要拆」的咨询；
- 不把模型 API、function calling 或 pi 的执行循环写成本人原创。

---

## 十二、推荐题目

首选：

> **基于任务感知动态模型路由的多智能体编程编排平台设计与实现**

备选：

> 面向成本优化的异构模型多智能体编程调度系统设计与实现

> 基于成本—质量约束的多智能体编程动态路由方法研究

首选题目同时覆盖平台实现与研究方法，并且不把论文绑定在某一个商业模型上。

---

## 十三、一句话总结

> 本项目首先是一个可正常对话和编程的桌面 Coding Agent。入口用**规则**判断是否多智能体（不为此多调模型）；Skill 或明确指令可强制启用，ADAPTIVE 下高置信度可自动开启，灰区询问，其余单 Agent。进入多智能体后，分配器根据用户添加的模型价签与公开能力为子任务选角色、模型与工具；各子任务使用独立执行会话与异构模型时，由**外置项目记忆（L0～L4）**传递上下文，而非继承父 LLM 会话全文，并与测试、审查升级时的 EvidenceBundle 衔接。毕设以设计与实现、功能测试和固定案例演示为主，不要求大规模实验。

---

## 十四、参考依据（选题阶段）

- FrugalGPT：LLM 级联与成本优化，arXiv:2305.05176
- RouterBench：多 LLM 路由评测框架，arXiv:2403.12031
- RouteLLM：基于偏好数据的强弱模型动态路由，arXiv:2406.18665
- Agent-as-a-Router：面向编程任务、结合执行反馈和历史经验的路由，arXiv:2606.22902
- SWE-bench：真实仓库级软件工程任务评测，arXiv:2310.06770
- TypeSafe AI：Jev / System One Models 官方说明与 API 文档
- OpenAI 官方多智能体与 Agents SDK 文档
- Claude Code 官方子 Agent 文档
- Cursor 官方 Subagents 文档
- Gemini CLI 官方 Subagents 与 Policy Engine 文档
- OpenAI Agents API 官方 Multi-agent 文档
