# 毕设设计方案：任务感知动态模型路由的多智能体编程编排平台

> 版本 v1.4 · 2026-09-25  
> 关键词：多智能体编排、异构模型路由、成本优化、任务 DAG、反馈调度、本科毕设

---

## 1. 项目概述

### 1.1 项目定位

本项目设计并实现一个带有自适应多智能体编排能力的桌面 Coding Agent。普通问答和简单开发与正常 Coding Agent 一致；用户可以显式选择多智能体 Skill，也可以由入口路由器在任务复杂、可拆为多个独立工作流且预期收益高于协调开销时自动开启多智能体。

进入多智能体模式后，平台完成：

1. 分析仓库和需求；
2. 将需求拆解为带依赖关系的任务 DAG；
3. 分析每个子任务的类型、复杂度、风险和验收方式；
4. 动态选择 Agent 角色、模型、工具权限和执行策略；
5. 通过**外置项目记忆**在多执行会话、异构模型之间传递 L0～L4 上下文（见 §5.6），而非依赖子会话继承父 LLM 状态；
6. 调度多个 Agent 执行任务；
7. 通过自动测试和 Reviewer 验证结果；
8. 在失败时继续、升级模型、拆分任务或请求人工介入；
9. 汇总代码、测试结果、成本、延迟和执行轨迹。

### 1.2 执行模式与触发规则

系统有两种执行模式和一个过渡决策：

| 模式 | 触发条件 | 系统行为 |
|---|---|---|
| `SINGLE_AGENT` | 简单任务，或复杂但强串行／高冲突／净收益不足 | 主 Agent 直接对话和编程，不创建 DAG，不生成子 Agent，不调用子任务路由器 |
| `MULTI_AGENT` | 用户显式强制，或入口门控高置信度判断适合多智能体 | 启动 Planner、DAG、子任务路由、调度、验证与重新路由 |
| `ASK_USER` | 自动门控处于阈值灰区 | 仅建议用户选择，不预先创建 Planner、DAG 或子 Agent |

系统提供两类入口：

1. **显式强制**：用户像 DSH 选择 `agent-teams` 一样，从 Skill／命令菜单选择“多智能体编排”，或明确说“使用多智能体”“让多个 Agent 并行处理”；
2. **自适应判断**：普通请求先进入低开销 `ComplexityGate`，综合任务规模、可并行度、上下文隔离收益、修改冲突、依赖关系和预期成本收益作出决定。

门控结果**仅**由发送前可得的静态特征与确定性规则产生。**禁止**为判断是否启用多智能体而调用执行级主模型或读取整仓代码；灰区用一次 UI 确认即可，不预先创建 Planner 或子 Agent。

```text
if explicit_multi_agent_skill OR explicit_user_request:
    FORCE_MULTI
else if adaptive_mode_enabled:
    gate = RuleGate(user_text, @paths, open_tabs, skill_hints)   # 无 LLM
    if gate.independent_workstreams >= 2
       AND gate.file_overlap_low
       AND NOT gate.strong_serial_signal
       AND gate.confidence >= τ_conf:
        AUTO_MULTI
    else if gate.independent_workstreams >= 2 AND gate.uncertain:
        ASK_USER
    else:
        SINGLE_AGENT
else:
    SINGLE_AGENT
```

用户可在设置中选择 `ADAPTIVE`、`MANUAL_ONLY` 或 `SINGLE_ONLY`。推荐 `ADAPTIVE`：高置信度时自动开启，灰区询问；`MANUAL_ONLY` 只响应 Skill／明确指令；`SINGLE_ONLY` 禁止自动开启。

“任务很复杂”“涉及多个文件”“预计耗时较长”均只能作为特征，不能单独触发。复杂但高度串行、需要连续修改同一文件、子任务间依赖密集的工作仍应由单 Agent 或单个强模型执行。

多智能体模式默认只绑定当前任务：任务结束、取消或用户要求“返回普通模式”后退出；新任务重新经过入口门控。执行模式、触发来源、特征快照、评分、置信度、触发时间和退出原因必须写入审计日志。

### 1.3 核心目标

系统优化的不是单次 API 价格，而是：

> 在质量约束下最小化成功完成任务的预期总成本。

可形式化为：

```text
选择执行配置 x，使 ExpectedCost(task, x) 最小

约束：
P(success | task, x) ≥ 质量阈值 τ
模型支持任务所需工具和上下文
执行权限满足安全策略
```

成本是系统内部统计与论文**案例分析**用的指标，不要求用户为每个任务设置预算，也不在主界面展示突兀的“已用／上限”预算计数器。

### 1.4 最终交付物（本科毕设口径）

- 一个可运行的 Electron 桌面 Coding Agent 应用；
- 显式 Skill 与规则化入口门控（可选 `ADAPTIVE` / `MANUAL_ONLY` / `SINGLE_ONLY`）；
- 任务 DAG、动态 Agent 创建与**基于模型注册表的子任务分配**；
- 规则路由器 + 可选的 LLM／Jev 分配器（统一 `RoutingEngine` 接口，Jev 非必需）；
- 验证、升级与重新调度闭环；
- 集成在主对话右侧栏中的 DAG 与子任务对话界面；
- **功能测试、典型场景演示、运行日志与成本统计**（不做学术论文级大规模对照实验与消融）；
- 毕业论文（侧重需求、架构、关键模块实现与测试分析）及答辩演示案例。

---

## 2. 论文要回答的问题（设计目标，非大规模实证）

本科毕设以**系统设计与实现**为主，不要求跑 SWE-bench 规模的对照实验或统计显著的消融。论文用架构说明、模块实现、功能测试和 2～3 个固定演示案例说明方案可行；若时间允许，可做**同一任务下「全强模型 vs 动态分配」的轻量对比**（各跑 1 次，记录 Token／估算成本），作为讨论材料而非严格实验结论。

### 2.1 设计层面要说明清楚的问题

- **Q1**：进入多智能体后，如何把角色、模型、工具解耦，并按子任务特征与用户配置的模型价签／能力卡做分配？
- **Q2**：入口门控与内部分配如何分层，避免简单任务为「要不要拆」多付一轮模型调用？
- **Q3**：多执行会话与异构模型并存时，外置项目记忆（L0～L4）如何装配 prompt，并与 `EvidenceBundle` 升级路径衔接？
- **Q4**：PolicyGate、测试与 Reviewer 如何约束分配结果，并在失败时升级或重规划？
- **Q5**（可选讨论）：在演示案例中，动态分配相对「全部使用同一强模型」是否表现出更低的估算总成本或更合理的角色分工？

### 2.2 设计假设（论文中作为动机，不要求统计检验）

- 子任务级分配比「每个角色静态绑一个模型」更贴合任务难度差异；
- 门控用规则即可挡住大部分误开多智能体；灰区询问比自动全开更稳妥；
- 高风险写任务应被 PolicyGate 抬到更高模型档，而不是无条件 cheap-first；
- 分配器读取用户维护的 `ModelProfile`（价格 + 公开能力摘要 + 可选历史成功率）是可行且可解释的默认方案。

---

## 3. 系统与现有工作的关系

| 系统或工作 | 主要能力 | 本项目的关系 |
|---|---|---|
| pi | 轻量 Coding Agent 骨架 | 候选执行底座，提供单 Agent 工具循环与模型接入 |
| Multica | 多 Agent 项目管理与任务可视化 | 产品形态与生命周期参照 |
| Cursor Subagents | 按复杂度、范围、描述和上下文自动委派；也支持显式调用 | 自动入口门控与模型覆盖的产品对照 |
| Claude Code Subagents / Agent Teams | subagent 可自动匹配；完整团队默认关闭且强调独立并行任务 | 区分轻量委派和完整 DAG 的设计依据 |
| Gemini CLI Subagents | 自动匹配专家，也可用 `@` 强制；支持禁用和策略限制 | 自动与显式调用共存的策略对照 |
| OpenAI Agents API Multi-agent | 会话先显式启用 multi-agent，再由 Agent 选择是否委派 | 能力开关与任务级生成决策分离的对照 |
| OpenAI Agents SDK | 可为不同 Agent 配置模型与工作流 | 多模型 Agent 对照 |
| OpenAI Responses Multi-agent | 同一次多 Agent 请求共享模型 | 统一模型执行对照 |
| FrugalGPT / RouteLLM | 强弱模型级联和动态路由 | 成本优化方法依据 |
| RouterBench | 多模型路由评测 | 路由指标和实验设计依据 |
| Agent-as-a-Router | 编程任务的反馈式路由 | 最直接的相关研究与对标对象 |
| Jev | 快速结构化决策模型 | 可替换的路由决策引擎之一 |

本项目的差异化是：先用显式指令与复杂度收益评估共同决定是否值得进入多 Agent，再在该模式内部同时决定角色、模型、工具和执行策略，并由测试与审查形成反馈闭环。与仅凭 Agent 描述自动委派不同，本项目会记录结构化入口特征、决策理由和预期协调成本，用于解释与评测。

---

## 4. 总体架构

```text
┌────────────────────────────────────────────────────────────┐
│                  Electron 桌面交互层                        │
│ 主对话 · Skill/命令选择器 · 模型选择 · DAG 右侧栏 · 子对话  │
└───────────────────────────┬────────────────────────────────┘
                            ↓
                 ┌──────────────────────────────┐
                 │ AdaptiveOrchestrationGate    │
                 │ 显式 Skill/指令 + Complexity │
                 │ Parallelism + Net Benefit    │
                 └──────────────┬───────────────┘
                   SINGLE / ASK │ MULTI
              ┌─────────────────┘      └───────────────┐
              ↓                                    ↓
┌────────────────────────┐          ┌──────────────────────────┐
│ Single Agent Runtime   │          │ Orchestrator             │
│ 普通对话与代码工具循环  │          │ DAG · 状态机 · 依赖调度   │
└────────────┬───────────┘          └────────────┬─────────────┘
             │                                   ↓
             │                    ┌──────────────────────────────┐
             │                    │ 任务画像与智能路由层（核心） │
             │                    │ Feature · Registry · Router │
             │                    │ PolicyGate · History Store  │
             │                    └────────────┬─────────────────┘
             │                                   ↓
             │                    ┌──────────────────────────────┐
             │                    │ Multi-Agent Runtime          │
             │                    │ Planner / Explorer / Executor│
             │                    │ Tester / Reviewer / Integrator│
             │                    └────────────┬─────────────────┘
             │                                   ↓
             │                    ┌──────────────────────────────┐
             │                    │ 验证、反馈与重新调度         │
             │                    └────────────┬─────────────────┘
             └───────────────────────┬─────────┘
                                     ↓
┌────────────────────────────────────────────────────────────┐
│ 模型与执行适配层：Provider · Jev · Git · Shell · Audit Log │
└────────────────────────────────────────────────────────────┘
```

### 分层原则

- 只有 `FORCE_MULTI` 或通过高阈值的 `AUTO_MULTI` 才能启动编排器、DAG 和子任务路由层；
- Skill、自然语言和复杂度自动触发统一生成同一种 `OrchestrationDecision`，不维护三套执行逻辑；
- `ASK_USER` 阶段不允许预创建子 Agent，避免用户尚未确认就产生开销；
- Agent 角色不固定绑定某个模型；
- 路由模型只给出建议，不直接执行工具；
- 安全、运行上限和权限由确定性代码控制；
- 每次决策和状态转移都可以回放；
- pi、Jev 和模型供应商均通过适配层隔离，可被替换；
- **项目记忆与 LLM 会话分离**：编排状态在 `ProjectMemory`，执行骨架只负责单次 `prompt` 调用。

### 4.2 模型接入与凭据（功能板块：本机 OpenCodex 可选网关）

#### 4.2.1 背景与问题

用户往往在多个桌面 Coding Agent（如 Codex CLI、Cursor、Google Antigravity 等）中**各自完成登录**，订阅或账号能力分散在各客户端内部，**不会**以统一 API Key 的形式暴露给第三方应用。若毕设平台为每一种来源单独实现「读取某 App 登录态」或逆向其本地存储，则实现脆弱、合规风险高，且与「模型与执行适配层可替换」的分层原则冲突。

另一方面，开发者本机可能已安装 **OpenCodex（`ocx`）** 等**本机统一代理**：在用户授权下集中维护各来源的 OAuth／会话，对外提供 **loopback 上的 OpenAI 兼容 HTTP 端点**，并将多来源模型汇总为一份可导出的目录。本板块描述：TaskWeaver **如何在不冒充 Cursor/Codex 客户端的前提下**，把这类本机网关当作**可选模型提供方**接入 pi 模型运行时，从而扩展 `ModelProfile` 可选集合，支撑单 Agent 与多 Agent 子任务异构路由。

#### 4.2.2 设计目标

| 目标 | 说明 |
|---|---|
| **降低重复配置** | 用户已在 `ocx` 中登录的来源，毕设侧优先通过网关复用，而非在应用内重复录入大量 API Key |
| **保持边界清晰** | TaskWeaver 只连接 `127.0.0.1` 上的兼容端点；**不**读取 Cursor Keychain、**不**解析 Cursor 私有数据库 |
| **与注册表一致** | 经网关暴露的每个逻辑模型仍映射为 `provider/id`，进入用户「已添加模型」列表后，分配器与 PolicyGate 行为不变 |
| **与官方 OAuth 并列** | 对 OpenAI Codex 订阅，仍保留应用内 `openai-codex` OAuth 作为**不依赖第三方代理**的正式路径；OpenCodex 为**可选增强**，非唯一方案 |

#### 4.2.3 架构与数据流

```text
┌─────────────────────────────────────────────────────────────┐
│ TaskWeaver（Electron 主进程 · pi ModelRuntime）              │
│  models.json（custom provider）+ 加密凭据（loopback 占位符） │
└────────────────────────────┬────────────────────────────────┘
                             │ HTTP OpenAI-compatible
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ OpenCodex 本机代理（用户自行安装、启动，默认 loopback 端口）   │
│  ocx login / sync · 模型目录 · 路由到各上游                  │
└─────────────┬───────────────┬───────────────┬───────────────┘
              ↓               ↓               ↓
        Codex 订阅      Cursor 路由模型   Antigravity 等
        （chatgpt       （逻辑 id 如      （逻辑 id 如
         backend）        cursor/…）        google-antigravity/…）
```

OpenCodex 对 pi 生态提供 **`ocx export --client pi`**：生成 `models.json` 片段（典型自定义 provider 名为 `opencodex`，`baseUrl` 指向 `http://127.0.0.1:<port>/v1`，`apiKey` 为文档约定的 **loopback 占位符**，非真实厂商密钥）。TaskWeaver 将该片段合并到应用数据目录下的 `taskweaver/models.json`，并在自有 `credential-store` 中保存对应 provider 凭据后，刷新模型目录；用户从**候选列表**显式添加所需模型后，主对话与 DAG 子任务即可选用。

逻辑模型 id 常带**命名空间前缀**（实现随 `ocx sync` 变化），例如：`cursor/grok-4.6`、`google-antigravity/gemini-3.8-flash-medium`、`gpt-5.4` 等。论文与界面展示以 **id + 价签/档位** 为准，不把前缀等同于「官方 API 产品名」。

#### 4.2.4 与核心模块的关系

- **ModelProfile / 价签注册表**：网关模型与用户直连 API 的模型**同一套** `ModelProfile` 字段（tier、单价、能力摘要、`enabledForAllocation`）；价签可仍由 TaskWeaver Price Registry 与 pi 目录合并，缺省价签时允许用户手填或演示时仅用相对档位。
- **分配器与 PolicyGate**：不感知流量是否经 `ocx`；仅根据已添加且启用的 `model_id` 与任务风险标签决策。子任务异构路由时，不同 `cursor/…` 与 `google-antigravity/…` 仍视为不同 `ModelProfile`，外置记忆（§5.6）注入规则不变。
- **审计日志**：记录实际使用的 `model_id` 与 `provider`；若经本机代理，可在运行详情中标注 `gateway: opencodex`（可选字段），便于答辩时区分「直连 OAuth」与「本机网关」路径。

#### 4.2.5 实现阶段（与产品补齐计划对齐）

| 阶段 | 能力 | 状态定位 |
|---|---|---|
| **P0（设计 + 手动）** | 文档说明合并 `ocx export --client pi`、配置 loopback 凭据、`ocx health` 前置检查 | 答辩可演示 |
| **P1（半自动）** | 设置页检测 `runtime-port.json` / `ocx health`，一键合并模型目录并写入凭据 | 产品增强 |
| **P2（运维）** | 启动 TaskWeaver 时可选 `ocx ensure`；同步失败时提示补登 `ocx login` | 可选 |

**不在 M1～M3 关键路径上阻塞**；与 MCP 设置页、工作区信任等同属「完整桌面 Agent 体验」补齐项。

#### 4.2.6 风险、合规与论文表述

- **运行时依赖**：未启动 `ocx` 时，经网关配置的模型不可用；需在 UI 或文档中明确「先启动本机代理」。
- **目录漂移**：上游或 `ocx sync` 变更后，模型列表需重新导出或触发 P1 同步；已添加但已下线的 id 应标记不可用。
- **工具与推理兼容**：经代理的路由模型对 function calling、推理字段的兼容因上游而异，需在演示案例中实测，不假设与直连 API 完全一致。
- **合规**：本方案描述的是用户**自愿安装**的本地网关，论文中表述为「可选的本机统一接入层」，**不**声称与 Cursor、Google 或 OpenAI 存在官方联合集成；订阅与计费仍受各上游服务条款约束。

---

## 5. 核心数据模型

### 5.1 编排入口决策 OrchestrationDecision

```json
{
  "mode": "multi_agent",
  "decision": "auto_multi",
  "source": "complexity_gate",
  "user_policy": "adaptive",
  "scope": "current_task",
  "scores": {
    "independent_workstreams": 3,
    "file_overlap_low": true,
    "strong_serial_signal": false,
    "context_isolation_gain": 0.74,
    "gate_confidence": 0.89
  },
  "reasons": ["前后端与测试可独立", "路径前缀重叠低", "无强串行信号"],
  "created_at": "2026-09-24T14:08:00+08:00"
}
```

`source` 取 `skill_menu`、`explicit_user_request` 或 `complexity_gate`。前两者生成 `force_multi`；第三种必须满足自动阈值，否则只能生成 `ask_user` 或 `single_agent`。所有入口最终使用同一状态机和审计格式。

### 5.2 任务描述 TaskSpec

```json
{
  "id": "T-004",
  "description": "实现登录失败次数限制",
  "type": "implementation",
  "dependencies": ["T-001", "T-002"],
  "estimated_files": 4,
  "cross_module": true,
  "risk_tags": ["security", "state_consistency"],
  "requires_write": true,
  "acceptance": ["unit_tests", "security_review"]
}
```

### 5.3 模型注册信息 ModelProfile

```json
{
  "id": "provider/model-name",
  "tier": "balanced",
  "input_price": 0,
  "output_price": 0,
  "context_window": 0,
  "supports_tools": true,
  "supports_reasoning": true,
  "supported_roles": ["planner", "executor", "reviewer"],
  "historical_success": {
    "code_search": 0.94,
    "implementation": 0.72,
    "security_review": 0.51
  }
}
```

模型价格和能力从配置读取，不在论文代码中写死具体产品名称，避免价格与版本变化造成系统失效。

### 5.4 路由结果 RouteDecision

```json
{
  "task_id": "T-004",
  "role": "executor",
  "model_id": "provider/model-name",
  "strategy": "balanced_direct",
  "tools": ["read", "edit", "bash"],
  "reasoning_effort": "medium",
  "max_tokens": 8000,
  "max_tool_calls": 20,
  "confidence": 0.84,
  "reasons": ["跨模块", "有测试", "涉及安全状态"],
  "fallback": "strong_executor"
}
```

### 5.5 执行证据 EvidenceBundle

升级或重新调度时不传递完整对话，而是传递经过压缩和验证的证据：

```json
{
  "task_id": "T-004",
  "related_files": ["src/auth/service.ts"],
  "verified_facts": ["连续失败计数写入Redis"],
  "diff": "...",
  "failed_tests": ["auth_lockout_test"],
  "error_summary": "并发请求下计数丢失",
  "attempted_actions": ["将GET+SET改为事务，测试仍失败"]
}
```

只有客观事实、必要 diff、错误和已验证结果进入升级上下文，未验证推理不作为事实传递。

### 5.6 外置项目记忆 ProjectMemory

编排层维护与第三方执行会话（pi `SessionManager` 等）**解耦**的项目记忆，绑定 `conversationId`（及可选 `workspacePath`）。子 Agent **不**假设继承父会话的 LLM 内部状态；每次 `AgentInstance` 创建前，由 `ContextAssembler` 从记忆中读取规定层级并写入 prompt，执行结束后由 `MemoryWriter` 写回。

#### 5.6.1 为何需要外置记忆

| 现象 | 说明 |
|---|---|
| 多执行会话 | Planner、各子任务、主对话汇总可能对应**多个独立会话文件** |
| 异构模型路由 | 子任务使用不同 `model_id` 时，无法共享同一 API 会话或缓存前缀 |
| 全量继承父聊天 | Token 膨胀、噪声增加，且破坏「按子任务裁剪上下文」的设计目标 |

外置记忆是**平台侧的真实状态**；LLM 会话仅是**无状态的推理调用**，每次携带从记忆中裁剪的片段。

#### 5.6.2 记忆分层（L0～L4）

| 层 | 字段示例 | 更新时机 | 注入策略 |
|---|---|---|---|
| **L0** | `user_goal` | 编排开始，来自用户消息 | 所有子任务固定前缀 |
| **L1** | `rolling_summary` | 每个子任务结束或主对话里程碑 | 所有子任务共享同一段（利于稳定前缀） |
| **L2** | `dependency_outputs[task_id]` | 依赖任务完成 | 仅 `dependsOn` 包含该 id 的任务 |
| **L3** | `facts`（文件、命令、测试结果） | 工具执行后规则提取 | 按 `TaskSpec.type` 选择性注入 |
| **L4** | `evidence_bundles[]` | 失败、升级、重试 | 仅 `ESCALATE` / 重规划路径；结构同 §5.5 |

**禁止**默认将父会话完整 message 列表作为 L1；必要时仅追加**已压缩摘要**。

#### 5.6.3 数据模型（示意）

```json
{
  "conversation_id": "uuid",
  "workspace_path": "/path/to/repo",
  "user_goal": "为登录增加失败次数限制并补测试",
  "rolling_summary": "已定位 auth 模块；T1 完成路径扫描…",
  "dependency_outputs": {
    "T1": { "text": "…", "files": ["src/auth/service.ts"] }
  },
  "facts": [
    { "kind": "file_touched", "path": "src/auth/service.ts", "verified": true }
  ],
  "evidence_bundles": [],
  "updated_at": "2026-09-24T17:00:00+08:00"
}
```

持久化路径建议：`userData/taskweaver-agent/memory/{conversationId}.json`（与编排 `orchestration/` 目录并列，纳入审计）。

#### 5.6.4 与执行流程的衔接

```text
OrchestrationService.planAndExecute
  ├─ MemoryStore.init(L0 from user text)
  ├─ Planner session（独立，noTools）→ TaskSpec[]
  ├─ AllocationRouter → RouteDecision[]
  └─ for each task in DAG order:
        ContextAssembler.build(L0, L1, L2*, L3*, TaskSpec, RouteDecision)
        → createAgentSession(model, prompt)   # 新会话 + 异构模型
        → on success: MemoryWriter.append(L1, L2, L3)
        → on failure: EvidenceBundle → L4, 触发 ESCALATE 策略
  └─ synthesize：主会话仅读 L1 + 子任务 outcome 列表
```

与选题思路 **§五** 表述一致。实现阶段本科可先落地 **L0 + L1 + L2**（L2 与当前「前置子任务结果」文本注入对齐），L3/L4 与 PolicyGate 升级同步迭代。

#### 5.6.5 单 Agent 与 Prompt Cache（说明性）

单 Agent 路径仅一个执行会话，连贯性由会话本身保证；外置记忆用于长对话压缩恢复（§8，可选）。多 Agent + 多模型时，**不追求**与「单端单会话长聊」相同的 Prompt Cache 命中率；通过 L1 固定块与稳定 system/tools 前缀，在同模型重复调用时仍可尽量利用服务商缓存。案例日志中的 `cacheReadTokens` / `cacheWriteTokens` 仅作**权衡说明**，不作为优化目标函数。

---

## 6. Agent 角色与动态配置

| 角色 | 职责 | 常用工具 | 可能使用的模型层级 |
|---|---|---|---|
| Planner | 需求分析、任务拆解、验收条件 | read、search | 中等或高能力 |
| Explorer | 代码定位、调用关系、资料收集 | read、grep、web | 低成本到高能力 |
| Executor | 代码实现和 Bug 修复 | read、edit、bash | 低成本到高能力 |
| Tester | 测试设计、测试实现和运行 | read、edit、bash | 低成本或中等 |
| Reviewer | 正确性、安全和回归审查 | read、diff、bash | 中等或高能力 |
| Integrator | 合并、冲突处理和全量验证 | git、bash | 程序优先，必要时调用模型 |

角色是一组 Prompt、工具和责任边界，不等于某一个固定 Agent 实例。每次任务执行时，平台通过 Agent Factory 动态创建实例：

```text
AgentInstance = RoleTemplate + SelectedModel + ToolPolicy + RunLimits
```

---

## 7. 路由机制

### 7.1 多智能体入口门控（规则层，不调用分配模型）

入口门控只回答「本任务是否值得使用多智能体」，**不负责**给每个子任务选模型。设计约束：

| 允许 | 禁止 |
|---|---|
| 解析用户文本、@ 路径、当前打开文件/目录前缀 | 为门控调用执行级主模型或 Planner |
| 关键词／模式识别独立工作流（如前端+后端+测试） | 为门控读取整仓并做深度代码分析 |
| 启发式判断文件簇重叠、强串行表述（同文件、先复现再改） | 用生成式模型自由文本直接创建团队 |
| 灰区弹出一次确认（`ASK_USER`） | 灰区预创建 DAG 或子 Agent |

**操作定义（`RuleGate` 输出字段，写入审计日志）：**

- `independent_workstreams`：从用户描述与显式路径中识别出的**低耦合工作簇**数量（至少 2 才可能 `AUTO_MULTI`）；
- `file_overlap_low`：各簇涉及路径前缀/文件名交集是否低于阈值（可用简单集合交集，不必 AST）；
- `strong_serial_signal`：是否出现同文件连续修改、强依赖逐步调试等表述或路径单一；
- `context_isolation_gain`（可选布尔）：是否预期大量搜索/日志会污染主对话（仅作加分，不单独触发）；
- `confidence`：上述规则一致程度（全满足为高，部分满足为灰区）。

`AUTO_MULTI` 条件：`independent_workstreams >= 2` **且** `file_overlap_low` **且** `NOT strong_serial_signal` **且** `confidence >= τ_conf`。不满足但工作流数 ≥ 2 且存在冲突/串行不确定 → `ASK_USER`；其余 → `SINGLE_AGENT`。`FORCE_MULTI` 来自 Skill 或明确指令，不受阈值限制；Planner 仍须提示明显文件冲突。

文档中的「净收益」仅作**概念解释**（并行收益 − 协调开销 − 冲突惩罚），实现阶段用上表代理字段代替可学习的 `w1…w6`。

### 7.2 子任务模型分配（模型注册表 + 分配器）

**只有** `OrchestrationDecision` 为 `force_multi` 或 `auto_multi` 之后，才进入子任务模型分配。这是你设想中「看已添加模型、价格和公开能力来分配」的落点，与门控严格分离。

**用户侧模型注册表（`ModelProfile`）**  
用户在设置中添加多个模型端点，每条记录包含：标识、档位（cheap/balanced/strong）、输入/输出单价、上下文窗口、是否支持工具、**公开能力摘要**（厂商说明或自填）、可选 `historical_success`（本机跑过后由审计日志回填）。

**分配流程：**

```text
Planner 产出 TaskSpec[]（类型、风险标签、依赖、验收方式，不选模型）
   ↓
读取全部 ModelProfile + 当前任务特征
   ↓
AllocationRouter（默认 RuleRouter；可选 LLMRouter / JevRouter）
   → 输出 RouteDecision[]（结构化 JSON，与 5.4 一致）
   ↓
PolicyGate 修正违规项（安全写入、Reviewer、上限等）
   ↓
Agent Factory 创建实例
```

**默认推荐（本科实现路径）：**

1. **RuleRouter**：按任务类型 + 风险标签 + 档位映射表分配（可解释、无额外 API）；
2. **LLMRouter（可选）**：用**单独的轻量分配模型**（或与执行主模型分离的一次调用），输入仅为 `TaskSpec[]` + `ModelProfile[]`，强制输出 `RouteDecision` Schema；**不是**让正在写代码的执行主模型兼任分配器，避免单 Agent 路径也付分配费；
3. **JevRouter（可选增强）**：接口与 LLMRouter 相同，服务不可用时回退 RuleRouter。

分配目标（写入 Prompt 或规则）：在通过 PolicyGate 的候选中，优先满足验收与风险约束，再在可行集合内倾向**估算期望成本更低**的模型；冷启动用价签与公开能力摘要，有历史则用 `historical_success`。期望成本仍可用选题中的 `E = C_l + (1-p)(C_h + C_t)` 作说明，其中 `p` 来自历史或档位先验，不要求毕设阶段拟合 `p` 的精确值。

单 Agent 模式（`SINGLE_AGENT`）下：**不调用** `AllocationRouter`，执行主模型直接完成任务。

### 7.3 候选执行策略

| 策略 | 适用任务 | 处理方式 |
|---|---|---|
| cheap_direct | 搜索、整理、机械性修改 | 低成本模型直接执行并验证 |
| balanced_direct | 常规功能和普通 Bug | 中等模型直接执行 |
| strong_direct | 架构、安全、并发、复杂调试 | 高能力模型直接执行 |
| scout_then_strong | 前期搜索简单、最终决策复杂 | 低成本探索，高能力实现 |
| deterministic | 格式化、Git 操作、固定检查 | 不调用模型，使用程序执行 |
| human_required | 高风险或需求信息不足 | 暂停并请求人工决策 |

### 7.4 硬规则示例

- 安全、认证、权限和并发任务不得路由至最低等级模型直接写入；
- 路由置信度低于阈值时选择更保守的模型或先执行只读探索；
- 没有自动验收条件的写任务至少需要 Reviewer；
- 调用次数、Token 或重试次数超过系统运行上限时暂停或重新规划；
- 危险命令不能由路由模型直接放行；
- 确定性工具能够完成的工作不调用生成式模型。

### 7.5 Jev 与 LLM 分配器（可选）

Jev／LLM 仅用于 **7.2 子任务分配**，不用于入口门控。输入为 `TaskSpec`、精简后的 `ModelProfile` 列表与策略位；输出为限定 Schema 的 `RouteDecision`。不直接读整仓、不写业务代码、不持有工具权限；仓库相关事实由 Planner／Explorer 写入 `TaskSpec` 或 `EvidenceBundle`。

- **RuleRouter**：默认实现，毕设答辩的保底路径；
- **LLMRouter**：实现「按价签与能力卡分配」的产品设想，与执行主模型解耦；
- **HistoryRouter**：根据审计日志中的成功率与成本做离线加权（增强项，可砍）。

---

## 8. 状态机与任务生命周期

状态机不再作为唯一创新点，而是自动路由平台的运行基础。

### 8.1 系统级状态

```text
IDLE → GATE ───────────────→ SINGLE_AGENT ───────────────────→ DONE
         │
         ├─→ ASK_USER ─────→ SINGLE_AGENT / ANALYZE
         │
         └─[FORCE_MULTI 或 AUTO_MULTI]
                    ↓
                  ANALYZE → PLAN → PROFILE → ROUTE → EXECUTE → VERIFY → INTEGRATE → DONE
                                  ↑          │
                                  │          ├→ RETRY
                                  │          ├→ ESCALATE
                                  └──────────┴→ REPLAN

任意阶段 → HUMAN_CONFIRM / FAILED
```

`IDLE → ANALYZE` 不允许直接发生，必须存在结果为 `force_multi` 或 `auto_multi` 的 `OrchestrationDecision`。普通任务沿 `GATE → SINGLE_AGENT → DONE` 执行，不进入任务级生命周期；灰区在用户选择前停留于 `ASK_USER`。

| 状态 | 含义 |
|---|---|
| GATE | 提取入口特征，计算是否值得启用多智能体 |
| ASK_USER | 处于自动阈值灰区，等待用户选择单 Agent 或多 Agent |
| SINGLE_AGENT | 主 Agent 按普通 Coding Agent 流程完成对话与代码任务 |
| ANALYZE | 多智能体入口守卫通过后，收集仓库、需求和环境信息 |
| PLAN | 生成任务 DAG 与验收条件 |
| PROFILE | 提取任务复杂度、风险和工具需求 |
| ROUTE | 选择角色、模型、策略和运行上限 |
| EXECUTE | Agent 执行任务 |
| VERIFY | 测试、静态检查和 Reviewer 验证 |
| RETRY | 同模型小范围修正 |
| ESCALATE | 更换模型或角色，携带 EvidenceBundle |
| REPLAN | 拆小或调整 DAG |
| INTEGRATE | 合并代码并执行全量测试 |

### 8.2 任务级生命周期

```text
queued → profiled → routed → running → verifying
                                     ├→ passed → integrated
                                     ├→ retrying → running
                                     ├→ escalating → routed
                                     ├→ replanning → queued
                                     └→ blocked / failed
```

每次转移记录：

```text
task_id, from, to, event, router, model, cost, timestamp, evidence_id
```

---

## 9. 验证与重新调度

验证优先使用客观信号：

1. 单元测试和项目测试；
2. 类型检查、编译和 lint；
3. 任务指定的验收脚本；
4. Reviewer 对正确性、安全性和回归风险的结构化判断。

失败后不默认升级，先分类原因：

| 失败类型 | 处理策略 |
|---|---|
| 命令或依赖环境错误 | 修复环境，不升级模型 |
| 小范围语法或格式错误 | 原模型限次重试 |
| 对需求理解错误 | 回到 PLAN 或 PROFILE |
| 任务复杂度低估 | 升级模型或拆分任务 |
| 缺少上下文 | 先调用 Explorer 补充证据 |
| 安全或架构问题 | 直接切换高能力模型和 Reviewer |
| 多次失败 | 停止自动尝试，请求人工处理 |

每个任务设置最大重试次数、升级次数、Token 和工具调用上限，防止无限循环。这些是系统保护参数，不作为用户界面的预算计数器。

---

## 10. 并行执行与代码隔离

- 每个写任务使用独立 Git worktree；
- 只读 Explorer 可以共享主仓库快照；
- 编排器按 DAG 依赖调度可并行任务；
- 确定性 Git 合并优先于 Integrator Agent；
- 合并冲突先分析涉及任务的意图和测试，再决定自动处理或人工介入；
- 集成完成后运行全量测试。

并行度不是越高越好。平台应避免让多个 Agent 同时修改相同文件或共享可变状态。

---

## 11. 用户界面

最终产品为 Electron 桌面应用，基础形态与正常 Coding Agent 一致，而不是把“主对话”和“DAG 看板”做成两个并列模块。

### 11.1 默认单 Agent 界面

- 主区域始终是普通 Coding Agent 对话、代码操作记录和底部输入框；
- 输入框提供 Skill／命令选择入口，可通过 `/` 或添加按钮选择；
- 入口门控未进入多智能体时，不展示 DAG、子任务数量或自动路由状态；
- 模型使用下拉菜单选择，不通过连续点击轮换；
- 主界面不展示“已花费／预算上限”计数器。

### 11.2 多智能体启用状态

- Skill 菜单提供类似 DSH `agent-teams` 的“多智能体编排”项；
- 手动选中后，输入框附近显示可撤销的 Skill 标识，让用户在发送前知道本次请求将启用多个 Agent；
- 用户明确输入“使用多智能体”时，显示同样的显式启用状态；
- 复杂度门控自动开启时，显示“多智能体 · 自动启用”，同时给出简短原因，例如“检测到前端、后端和测试三个低冲突工作流”，并提供“改为单 Agent”；
- 灰区不直接开启，而是显示一次紧凑建议，用户确认后才创建 DAG；
- 生成 DAG 后才出现右上角“显示／隐藏任务侧边栏”按钮和任务数量；
- 多智能体任务结束或用户退出后，启用状态和 DAG 入口一并清除。

### 11.3 DAG 与子任务对话

- 点击右上角按钮，在当前主对话右侧展开 DAG 侧边栏，主对话仍然保留；
- DAG 不是独立页面，也不占用左侧导航中的单独模块；
- 点击 DAG 中的某个任务时，只将右侧栏原地替换为该子任务的独立对话；
- 子任务对话提供“返回 DAG”和“关闭侧栏”，关闭后仍停留在主对话；
- 侧边栏展示角色、所选模型、状态、路由原因、日志、测试与审查结果；成本数据放在实验或运行详情中，不作为主界面的预算提醒。

界面既服务于正常开发，也为多智能体运行提供可观察性和人工接管能力，但不扩展为完整团队项目管理系统。

---

## 12. 测试与验证方案（本科毕设）

毕设验收重点是：**能跑、逻辑对、关键路径可演示、论文能讲清设计与实现**。不要求复现论文级基准、多轮对照或统计显著性结论。

### 12.1 功能测试

| 类别 | 内容 |
|---|---|
| 单 Agent | 问答、读文件、改代码、跑测试；确认未调用门控后的分配器、无 DAG |
| 门控 | 下表固定用例；检查 `OrchestrationDecision` 与审计日志字段 |
| 多智能体 | Skill 强制进入后：Planner → DAG → 分配 → 执行 → 验证；侧栏与状态清理 |
| 分配 | 修改 `ModelProfile` 价签/档位后，同一条 `TaskSpec` 的 `RouteDecision` 是否变化；PolicyGate 是否拦截低风险档写安全任务 |
| 反馈 | 模拟测试失败，是否按失败类型重试/升级/EvidenceBundle |

### 12.2 门控用例（手工验收即可）

| 输入 | 预期模式 | 预期行为 |
|---|---|---|
| “解释这个函数” | SINGLE_AGENT | 无 DAG；门控无 LLM 调用记录 |
| “修复这个报错并运行测试” | SINGLE_AGENT | 主 Agent 直接编辑 |
| Skill 选择多智能体后提交 | MULTI_AGENT | 创建 DAG |
| “使用多智能体并行完成…” | MULTI_AGENT | `FORCE_MULTI` |
| 描述含前端+后端+独立测试、路径前缀不重叠 | AUTO_MULTI（若 ADAPTIVE） | 展示原因与撤销 |
| 同文件、逐步复现的复杂 Bug | SINGLE_AGENT | 不拆分 |
| 可并行但路径/表述含糊 | ASK_USER | 不预创建 DAG |

### 12.3 演示案例（论文第 7 章素材）

准备 **2～3 个固定场景**（建议自备小仓库或课程项目片段），例如：

1. 登录失败次数限制 + 测试（多子任务、含安全标签）；
2. 跨模块小功能（展示 Explorer + Executor + Tester 分工）；
3. 简单单文件 Bug（展示门控保持 SINGLE_AGENT）。

每个案例记录：执行模式、DAG 截图、各子任务 `RouteDecision`、测试是否通过、审计日志中的 **Token/估算成本合计**（从 API 返回或本地计数器汇总）。

### 12.4 可选轻量对比（非必需）

若 API 预算允许，对**同一演示案例**再跑一遍「所有子任务均用强模型」，与动态分配各 **1 次**，在论文中用表格对比总 Token/估算成本与是否通过测试，并说明样本量为 1、仅供参考。不做消融、不计算 Cost Regret、不接入 SWE-bench 排行榜。

### 12.5 论文中如何写「测试与分析」

- 以功能测试用例表 + 演示案例轨迹为主；
- 相关工作中的 FrugalGPT、RouterBench 等作**理论背景**，不声称本毕设复现其实验；
- 诚实写局限：规则门控误判、分配器冷启动、模型随机性等。

---

## 13. 技术选型

| 模块 | 候选选择 | 说明 |
|---|---|---|
| 主语言 | TypeScript | 与 pi SDK 同语言，适合事件和平台开发 |
| Agent 骨架 | pi 或自定义适配器 | 复用单 Agent 工具循环，不复用编排逻辑 |
| 编排状态 | SQLite + JSONL 审计日志 | SQLite 保存当前状态，JSONL 用于回放和实验 |
| 路由决策 | RuleRouter（默认）+ 可选 LLMRouter / JevRouter | 仅用于子任务分配；门控为纯规则 |
| 并行隔离 | Git worktree | 标准、可追踪、易于合并 |
| 验证 | 项目原生测试 + lint/typecheck | 以客观信号为主 |
| 桌面界面 | Electron + React + TypeScript | Codex 式主对话，按需展开 DAG 右侧栏 |
| 模型凭据（可选） | 应用内 OAuth（如 `openai-codex`）+ 本机 OpenCodex loopback | 官方订阅走 OAuth；多来源汇总走用户自建的 `ocx` 网关，见 §4.2 |

---

## 14. 最小可行版本与里程碑

### M1：单任务闭环（第 1～2 周）

- 完成 Electron + React 桌面壳、主对话和模型选择；
- 接入一个 Agent 骨架和两个不同成本层级的模型；
- 完成统一模型适配接口；
- 跑通读取仓库、修改代码、运行测试。

### M2：任务拆解与静态编排（第 3～4 周）

- 实现 Skill／命令选择器与 `AdaptiveOrchestrationGate`；
- 跑通 `FORCE_MULTI`、`AUTO_MULTI`、`ASK_USER` 和 `SINGLE_AGENT` 四类入口决策；
- Planner 生成 TaskSpec 和 DAG；
- 实现角色模板与 Agent Factory；
- 实现状态持久化和审计日志。

### M3：动态分配（第 5～7 周）

- 实现 Model Registry（用户添加模型与价签/能力卡）与 RuleRouter；
- 可选接入 LLMRouter（按 ModelProfile 分配）；
- 实现 PolicyGate 与三类核心执行策略。

### M4：验证反馈闭环（第 8～9 周）

- 接入测试、Reviewer 和失败分类；
- 实现重试、升级、重新规划和 EvidenceBundle；
- 加入 Token、工具调用和循环上限；
- 实现 `ProjectMemory`（L0～L2）与 `ContextAssembler`，替换「仅拼接前置任务原文」的临时方案。

### M5：并行与展示（第 10～11 周）

- Git worktree 隔离；
- DAG 并行调度与集成；
- 完成右侧 DAG、节点切换子任务对话、返回与收起交互。

### M6：测试与论文（第 12～16 周）

- 完成功能测试与 2～3 个固定演示案例；
- 整理审计日志、截图与可选轻量成本对比；
- 撰写论文与答辩演示。

### 范围收缩顺序

若进度不足，按以下顺序砍功能：

1. 取消非核心统计图表和高级动效，但保留桌面主对话、自适应门控与 DAG 侧边栏；
2. 并行度固定为 1；
3. Integrator 改为确定性 Git 脚本；
4. 历史学习改为离线统计；
5. 保留规则分配、验证反馈与演示案例；砍掉 LLM/Jev 分配器与 AUTO 门控（仅保留 Skill 强制多智能体）。

---

## 15. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 自动入口门控误判或状态未清理 | 普通任务产生额外调用，或复杂任务未并行 | 高低双阈值、独立工作流硬条件、灰区询问、可撤销状态和门控回归测试 |
| Jev 服务新、闭源、可能不稳定 | 核心路由不可用 | 统一接口，RuleRouter 随时替代，锁定版本和请求格式 |
| 路由判断缺少仓库信息 | 难度低估 | 静态特征提取或先执行限额只读探索 |
| 便宜模型失败造成重复成本 | 成本反而上升 | 不采用无条件 cheap-first；用期望成本和风险选择策略 |
| 失败上下文污染强模型 | 降低升级效果 | 只传 EvidenceBundle（L4），允许从干净 worktree 重做 |
| 子会话间上下文断裂 | 子 Agent 跑偏或重复劳动 | 外置记忆 L0～L2 强制注入；L1 滚动摘要写回 |
| 模型价格和能力快速变化 | 结果难复现 | 记录模型精确版本、日期、价格快照和参数 |
| 多模型结果随机性较大 | 演示案例结果不稳定 | 固定 Prompt/任务描述，论文中说明单次运行局限 |
| 公开基准环境复杂 | 不适合本科周期 | 不做 SWE-bench 全量，仅用自备小仓库演示 |
| 平台范围过大 | 无法完成 | 以路由闭环为核心，界面和复杂并行均为次要功能 |
| 本机 OpenCodex 未运行或目录过期 | 网关模型不可用或调用失败 | 健康检查 + 设置页提示；保留直连 OAuth/API Key 路径；演示前固定 `ocx sync` |

---

## 16. 贡献点与归属

| 内容 | 归属 |
|---|---|
| Coding Agent 工具执行循环 | pi 或其他第三方骨架 |
| 模型生成和 function calling | 模型供应商能力 |
| Jev 的结构化决策能力 | TypeSafe AI |
| 任务特征定义与 TaskSpec | 本项目 |
| 角色、模型、工具解耦机制 | 本项目 |
| RoutingEngine 与多路由器适配 | 本项目 |
| 成本—质量策略与 PolicyGate | 本项目 |
| 验证反馈、EvidenceBundle 与重新调度 | 本项目 |
| 外置项目记忆（ProjectMemory L0～L4）与 ContextAssembler | 本项目 |
| 显式 Skill 与复杂度收益结合的自适应入口门控 | 本项目 |
| 状态机、DAG 调度、审计和运行上限控制 | 本项目 |
| 功能测试、演示案例与运行成本统计 | 本项目 |

论文中必须如实说明第三方能力，创新点集中在任务建模、门控与分配分层、动态分配与反馈闭环；**不要求**大规模实证。

---

## 17. 论文建议结构

1. 绪论：多 Agent 编程的成本问题与研究目标；
2. 相关工作：Coding Agent、多智能体、LLM 路由和级联；
3. 需求分析与总体架构；
4. 任务画像与动态模型路由方法；
5. 多 Agent 编排、验证反馈与状态管理；
6. 系统实现；
7. 系统测试与案例分析；
8. 局限性、总结与展望。

论文核心章节应是第 4 章（路由与分配方法）、第 6 章（实现）和第 7 章（测试与案例）；界面实现从简叙述即可。

---

## 18. 参考文献与资料

### LLM 路由与成本优化

- Chen, Zaharia, Zou. **FrugalGPT: How to Use Large Language Models While Reducing Cost and Improving Performance.** arXiv:2305.05176.
- Hu et al. **RouterBench: A Benchmark for Multi-LLM Routing System.** arXiv:2403.12031.
- Ong et al. **RouteLLM: Learning to Route LLMs with Preference Data.** arXiv:2406.18665.
- Zhou et al. **Agent-as-a-Router: Agentic Model Routing for Coding Tasks.** arXiv:2606.22902.

### Coding Agent 与评测

- Jimenez et al. **SWE-bench: Can Language Models Resolve Real-World GitHub Issues?** arXiv:2310.06770.
- Yang et al. **SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering.** arXiv:2405.15793.
- Chen et al. **Agentless: Demystifying LLM-based Software Engineering Agents.** arXiv:2407.01489.
- Hong et al. **MetaGPT: Meta Programming for A Multi-Agent Collaborative Framework.** arXiv:2308.00352.
- Qian et al. **ChatDev: Communicative Agents for Software Development.** arXiv:2307.07924.

### 产品与官方文档

- TypeSafe AI：Jev 与 System One Models 官方介绍、API 文档；
- OpenAI：[Agents API Multi-agent](https://developers.openai.com/api/docs/guides/agents-api/multi-agent)、Responses Multi-agent、Agents SDK 模型配置文档；
- Anthropic：[Claude Code Subagents](https://code.claude.com/docs/en/sub-agents)、[Agent Teams](https://code.claude.com/docs/en/agent-teams)；
- Cursor：[Subagents](https://cursor.com/docs/subagents)；
- Google：[Gemini CLI Subagents](https://geminicli.com/docs/core/subagents/)、[Policy Engine](https://geminicli.com/docs/reference/policy-engine/)；
- pi：项目 README、SDK 与 RPC 文档；
- Multica：产品和开源项目文档。

---

## 19. 最终摘要

> 本项目实现一个带自适应多智能体能力的桌面 Coding Agent。入口门控采用**纯规则**判断是否启用多智能体（不调用分配模型）；用户可通过 Skill 或明确指令强制启用，或在 ADAPTIVE 下由规则高置信度自动开启、灰区询问，其余保持单 Agent。进入多智能体后，Planner 产出子任务列表，**分配器**读取用户配置的模型价签与公开能力（默认 RuleRouter，可选 LLM/Jev），为每个子任务选择角色、模型与策略，并经 PolicyGate、测试与审查形成重新调度闭环。本科毕设以系统实现与功能测试、固定演示案例为主，不做大规模对照实验。
